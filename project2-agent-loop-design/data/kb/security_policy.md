# Security Baseline Policy

All servers must run a supported OS version. Any asset with status "unsupported_os" is a critical finding and must be flagged in every asset report.

Remote access to production servers requires MFA and a recorded change ticket. Local administrator accounts are disabled by default on all laptops.

Asset reports that include cost figures must state amounts in both USD and NGN using the official rate at report time.
